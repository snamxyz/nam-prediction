{
  "name": "@nam-prediction/matching-engine",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "bun run dist/server.js",
    "test": "bun test src"
  },
  "dependencies": {
    "@nam-prediction/shared": "workspace:*",
    "elysia": "^1.2.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}